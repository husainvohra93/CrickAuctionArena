# Neon / Postgres URL
# Example (Neon):
# postgresql://neondb_owner:password@ep-dawn-lab-.../neondb?sslmode=require&channel_binding=require
# Note: include the query params (e.g., sslmode=require) for Neon; URL-encode special characters in the password (e.g., @ -> %40).
# Keep real credentials out of source control (use .env.local or your deployment provider's secrets).
DATABASE_URL=postgresql://username:password@host:port/dbname?sslmode=require&channel_binding=require

# Server configuration
# Server port (optional; defaults to 4000 in the server code)
PORT=4000
NODE_ENV=development

# Public socket URL used by the Next.js frontend
# This value is exposed to the browser (public). Use a secure URL (https:// or wss://) in production.
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Admin credentials (dev only)
# WARNING: the current implementation returns the ADMIN_SECRET from the login endpoint as a static token.
# For production, consider using short-lived tokens (e.g., JWT) or a secrets manager instead of static secrets.
ADMIN_PASSWORD=changeme
ADMIN_SECRET=super-secret-token
